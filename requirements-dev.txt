-r requirements.txt

pytest>=8.0
pytest-cov>=5.0
pytest-mock>=3.12
mypy>=1.10
types-requests>=2.32

# Local pre-commit gate (ruff, mypy, smoke pytest); see .pre-commit-config.yaml.
pre-commit>=4.0

# Used only by evaluation/compare_sklearn.py for empirical
# comparison; not required to run the search engine itself.
scikit-learn>=1.5

# Property-based testing in tests/test_properties.py.
hypothesis>=6.100

# Phase A (dense retrieval): numpy is required for the dense ranker
# tests; sentence-transformers is opt-in (>~80 MB model download).
# Install sentence-transformers manually if you want the full dense
# retrieval pipeline:
#     pip install 'sentence-transformers>=3.0'
numpy>=1.26

# Phase B (learning-to-rank): training the LambdaRank model. On macOS
# the native runtime additionally requires libomp:
#     brew install libomp
# Tests skip gracefully if the lightgbm runtime can't load.
lightgbm>=4.0
